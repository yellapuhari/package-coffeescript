Description: Load coffeescript libraries from /usr/lib/coffeescript
 Relative path loading prevents coffee from being installed to /usr/bin
Forwarded: not-needed
Author: Geza Kovacs <gkovacs@mit.edu>
--- a/bin/cake
+++ b/bin/cake
@@ -2,7 +2,7 @@
 
 var path = require('path');
 var fs  = require('fs');
-var lib = path.join(path.dirname(fs.realpathSync(__filename)), '../lib');
+var lib = '/usr/lib/coffeescript';
 
 require(lib + '/helpers').helpers.extend(global, require('sys'));
 require(lib + '/cake').run();
--- a/bin/coffee
+++ b/bin/coffee
@@ -2,7 +2,7 @@
 
 var path = require('path');
 var fs  = require('fs');
-var lib = path.join(path.dirname(fs.realpathSync(__filename)), '../lib');
+var lib = '/usr/lib/coffeescript';
 
 require(lib + '/helpers').helpers.extend(global, require('sys'));
 require(lib + '/command').run();
